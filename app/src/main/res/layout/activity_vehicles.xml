<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorBackground">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_vehicles"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary"
        android:title="@string/title_fleet"
        android:titleTextColor="@color/white" />

    <!-- L9: Filter Bar -->
    <include layout="@layout/layout_filter_bar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/margin_item">

            <!-- Driver Card 1 -->
            <include
                layout="@layout/item_driver_card"
                android:id="@+id/card_driver_1" />

            <!-- Driver Card 2 -->
            <include
                layout="@layout/item_driver_card"
                android:id="@+id/card_driver_2" />

            <!-- Driver Card 3 -->
            <include
                layout="@layout/item_driver_card"
                android:id="@+id/card_driver_3" />

            <!-- Driver Card 4 -->
            <include
                layout="@layout/item_driver_card"
                android:id="@+id/card_driver_4" />

            <!-- Driver Card 5 -->
            <include
                layout="@layout/item_driver_card"
                android:id="@+id/card_driver_5" />

        </LinearLayout>
    </ScrollView>

</LinearLayout>

